spring:
  application:
    name: gateway-server
  cloud:
    nacos:
      config:
        server-addr: http://localhost:8848
        file-extension: yaml
      discovery:
        server-addr: http://localhost:8848
